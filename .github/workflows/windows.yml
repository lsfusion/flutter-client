name: Windows Build

on:
#  push:
#    branches: [ main ]
#  pull_request:
  workflow_dispatch:
  workflow_call:

jobs:
  build-windows:
    name: Build Windows
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Flutter
        uses: flutter-actions/setup-flutter@v4
        with:
          channel: stable
          version: latest

      - name: Install dependencies
        run: flutter pub get

#      - name: Build Windows app
#        run: flutter build windows --release

      - name: Create MSIX Installer
        run: dart run msix:create
        shell: pwsh

      - name: Setup CodeSignTool
        run: |
          mkdir CodeSignTool
          $url = "https://www.ssl.com/download/codesigntool-for-windows/"
          curl.exe -L $url -o CodeSignTool.zip
          if ((Get-Item CodeSignTool.zip).Length -lt 100000) {
            throw "Downloaded file is too small, likely not the tool. Check the URL."
          }
          Expand-Archive -Path CodeSignTool.zip -DestinationPath CodeSignTool
          $jarPath = Get-ChildItem -Path CodeSignTool -Filter "code_sign_tool-*.jar" -Recurse | Select-Object -First 1
          if ($null -eq $jarPath) {
            throw "CodeSignTool JAR not found in the extracted files."
          }
          # We need the directory where 'conf' and 'jar' folders are located
          $toolRoot = $jarPath.Directory.Parent.FullName
          echo "CODESIGNTOOL_JAR=$($jarPath.FullName)" >> $env:GITHUB_ENV
          echo "CODESIGNTOOL_ROOT=$toolRoot" >> $env:GITHUB_ENV
        shell: pwsh

      - name: Sign MSIX with CodeSignTool
        run: |
          $msixPath = Get-ChildItem -Path build/windows/x64/runner/Release/*.msix | Select-Object -First 1
          $absoluteMsixPath = $msixPath.FullName
          $absoluteOutputDir = Join-Path (Get-Location).Path "build/windows/x64/runner/Release/signed"
          
          cd "${{ env.CODESIGNTOOL_ROOT }}"
          java -jar "${{ env.CODESIGNTOOL_JAR }}" sign `
            -username="${{ secrets.SS_USER_NAME }}" `
            -password="${{ secrets.SS_USER_PASSWORD }}" `
            -credential_id="${{ secrets.SS_CREDENTIAL_ID }}" `
            -totp_secret="${{ secrets.SS_TOTP_SECRET }}" `
            -input_file_path="$absoluteMsixPath" `
            -output_dir_path="$absoluteOutputDir"
        shell: pwsh

      - name: Upload Windows build
        uses: actions/upload-artifact@v4
        with:
          name: windows-msix
          path: |
            build/windows/x64/runner/Release/*.msix
            build/windows/x64/runner/Release/signed/*.msix